source :rubygems

gem 'rake', :group => :rake

# Testing
group :test do
  gem 'simplecov', :require => false
  gem 'guard-minitest', '>= 0.5.0'
  gem 'minitest'
  gem 'turn', :git => 'git://github.com/japgolly/turn.git'
  gem 'awesome_print'
end
